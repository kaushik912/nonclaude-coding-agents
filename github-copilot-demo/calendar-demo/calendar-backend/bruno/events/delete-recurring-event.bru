meta {
  name: Delete Recurring Event
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/events/{{recurringEventId}}
  body: none
  auth: none
}

tests {
  test("delete recurring event returns 204", () => {
    expect(res.status).to.equal(204);
  });
}
